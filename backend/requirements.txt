# 后端直接依赖（在 backend 目录：pip install -r requirements.txt）
# 运行脚本前请先激活 venv：.\venv\Scripts\Activate.ps1

# API
flask>=3.0,<4
flask-cors>=4.0,<5

# MySQL（app/db.py、scripts/run_sql.py 等）
pymysql>=1.1,<2
# MySQL 8 默认 caching_sha2_password 时 PyMySQL 需要（否则报 cryptography package is required）
cryptography>=42,<46

# .env 加载（app/config.py）
python-dotenv>=1.0,<2

# Markdown 渲染（app/markdown_util.py）；文章详情可 ?html=1 返回 body_html
markdown>=3.5,<4
Pygments>=2.18,<3

# 中文关键词自动抽取（app/keywords_extract.py、scripts/extract_keywords.py）
jieba>=0.42,<2

# YAML front matter（app/md_import.py、scripts/content_tools/import_markdown_posts.py）
PyYAML>=6.0,<7

# 媒体图片 WebP 内容协商转码（app/media_webp.py）
Pillow>=10,<13
