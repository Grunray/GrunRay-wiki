/** 光标引擎只用 light/dark 两套线稿；站点 abstract 走 dark。 */
export type CursorTheme = 'light' | 'dark'

export interface MoonlitCursorAssets {
  columbinaUrl: string
  damseletteUrl: string
  wishUrl: string
  primogemUrl: string
  sleepUrl: string
  kuuhenkiUrl: string
}
