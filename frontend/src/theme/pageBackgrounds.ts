/**
 * 与页面背景一致，供主题切换扩散层使用。
 * 修改浅色渐变时请同步改 `src/styles/themes/tokens.light.css` 中的 `--color-bg-base`。
 */
export const LIGHT_MODE_PAGE_BACKGROUND =
  'linear-gradient(165deg, #f0f5f5 0%, #ccdddf 25%, #c1dec8 90%, #b3ced0 100%)'

/** 与 `tokens.dark.css` 中 `--color-bg-base` 一致 */
export const DARK_MODE_PAGE_BACKGROUND = '#172226'
