/**
 * 与页面背景一致，供主题切换扩散层使用。
 * 修改时请同步 `src/styles/themes/tokens.light.css` 中的 `--color-bg-base`。
 */
export const LIGHT_MODE_PAGE_BACKGROUND = '#f4f1e8'

/** 与 `tokens.dark.css` 中 `--color-bg-base` 一致 */
export const DARK_MODE_PAGE_BACKGROUND = '#0d1210'
